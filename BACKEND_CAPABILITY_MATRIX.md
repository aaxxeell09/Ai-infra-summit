# Backend capability matrix

Generated by `scripts/render_backend_capability_matrix.py` from
`turbo/optimizer/search_space.py`. Do not edit by hand: regenerate it.
This table is TurboLab's source of truth for what may be searched.

"Officially supported" means the frozen runner accepts the key and the native layer
does not reject it for that backend. "Actually wired" is a separate question: a
control can exist as a struct field and still have no path from configuration to
the runtime, which is exactly the case for the QAIRT sampler fields.

| control | llama_cpp_cpu | llama_cpp_htp | qairt_npu | officially supported | actually wired | requires reload | qualified search allowed | diagnostic only | evidence / code location |
|---|---|---|---|---|---|---|---|---|---|
| `max_tokens` | yes (16, 24, 32, 48, 64, 96, 128, 192, 256) | yes (16, 24, 32, 48, 64, 96, 128, 192, 256) | yes (16, 24, 32, 48, 64, 96, 128, 192, 256) | yes | config to runtime, verified by reading the consuming code | no | yes | no | eval/run_secretary_eval.py allows the key and records it in generation_protocol; turbo/native.py passes it to the generation config for every plugin |
| `stop_after_tool_call` | no | no | yes (False, True) | yes | config to runtime, verified by reading the consuming code | yes | yes | no | turbo/native.py:649 accepts it only when the resolved plugin is qairt and raises for any other plugin; docs/qairt-single-action.md records the mechanism |
| `threads` | yes (0, 2, 4, 6, 8, 10, 12) | yes (0, 2, 4, 6, 8, 10, 12) | no | yes | config to runtime, verified by reading the consuming code | yes | yes | no | turbo/native.py forwards it as n_threads; :658 rejects any nonzero value for qairt |
| `threads_batch` | yes (0, 2, 4, 6, 8, 10, 12) | yes (0, 2, 4, 6, 8, 10, 12) | no | yes | config to runtime, verified by reading the consuming code | yes | yes | no | turbo/native.py forwards it as n_threads_batch; :658 rejects it for qairt |
| `n_batch` | yes (0, 64, 128, 256, 512, 1024) | yes (0, 64, 128, 256, 512, 1024) | no | yes | config to runtime, verified by reading the consuming code | yes | yes | no | turbo/native.py forwards it as n_batch; :658 rejects it for qairt |
| `ubatch` | yes (0, 64, 128, 256, 512) | yes (0, 64, 128, 256, 512) | no | yes | config to runtime, verified by reading the consuming code | yes | yes | no | turbo/native.py forwards it as n_ubatch; :658 rejects it for qairt |
| `context` | yes (1024, 2048, 4096, 8192) | yes (1024, 2048, 4096, 8192) | no | yes | config to runtime, verified by reading the consuming code | yes | yes | no | turbo/native.py:656 requires qairt context to equal the compiled artifact context, so it is a knob for llama.cpp backends only |
| `spec_type` | yes ('none') | yes ('none') | no | yes | config to runtime, verified by reading the consuming code | yes | yes | no | turbo/native.py encodes it for the native generation config; :658 rejects any value other than none/empty for qairt |
| `draft_tokens` | declared, status unknown | declared, status unknown | no | unknown | forwarded as spec_n_max, but inert while spec_type is none, so it changes nothing today | yes | no | no | turbo/native.py forwards it as spec_n_max, but it is inert while spec_type is none, and no repository evidence shows a working speculative configuration |
| `grammar` | no | no | no | requires_protocol_extension | refused by the frozen runner before any runtime call; the pinned adapter would forward it | yes | no | yes | eval/run_secretary_eval.py rejects a truthy grammar: "ToolWire grammar is not valid for the production Secretary tool contract" |
| `temperature` | no | no | no | requires_protocol_extension | struct field exists in the ctypes generation config, no configuration path populates it | yes | no | yes | not in the frozen runner allowed-key set; generation_protocol hardcodes temperature 0 in eval/run_secretary_eval.py |
| `top_k` | no | no | no | requires_protocol_extension | struct field exists in the ctypes generation config, no configuration path populates it | yes | no | yes | not in the frozen runner allowed-key set; no path passes it from config to the native sampler |
| `top_p` | no | no | no | requires_protocol_extension | struct field exists, the adapter sends a fixed 1.0 and ignores configuration | yes | no | yes | not in the frozen runner allowed-key set; the ctypes adapter sends a fixed 1.0 |
| `seed` | no | no | no | requires_protocol_extension | struct field exists, the adapter sends a fixed -1 and ignores configuration | yes | no | yes | not in the frozen runner allowed-key set; the adapter sends -1 |

## Single-variable treatment ceiling per backend

One candidate changes exactly one field, because `turbo/campaign_plan.py` refuses a
treatment that differs from its control in more than one place. The ceiling below is
therefore a sum of enumeration sizes, not a Cartesian product.

| backend | supported controls | maximum single-variable treatments |
|---|---|---:|
| `llama_cpp_cpu` | `max_tokens`, `threads`, `threads_batch`, `n_batch`, `ubatch`, `context`, `spec_type` | 39 |
| `llama_cpp_htp` | `max_tokens`, `threads`, `threads_batch`, `n_batch`, `ubatch`, `context`, `spec_type` | 39 |
| `qairt_npu` | `max_tokens`, `stop_after_tool_call` | 11 |

The QAIRT figure is small and that is a finding, not a defect of the search engine.
High-volume search is genuinely available on the llama.cpp backends; on QAIRT the
honest answer is that the current contract exposes two controls.

## Lane C register

Controls and mechanisms the current contract cannot express. None of these may
reach a qualified experiment. They are recorded so the questions survive.

| item | missing capability | smallest safe extension | benchmark semantics | priority |
|---|---|---|---|---|
| `QAIRT_EXPLICIT_SAMPLER_CONTROL` | No path carries a sampler value from the evaluation config to the native sampler. eval/run_secretary_eval.py rejects any key outside its allowed set, so top_k, top_p, temperature and seed cannot even be written in a config file, and turbo/native.py exposes the sampler struct fields but the runner never populates them from config. | A versioned runner contract (not an in-place edit of v2) that accepts an optional sampler object, records requested and effective values in the report, and refuses to run when the effective values cannot be read back from the runtime. Requested values that the plugin silently overrides must surface as a contradiction, not as a success. | exposes_an_inference_control_only | first Lane C item to review after TurboLab V1 |
| `QAIRT_ACTION_GRAMMAR` | eval/run_secretary_eval.py rejects a truthy grammar key outright: "ToolWire grammar is not valid for the production Secretary tool contract". The pinned adapter does forward inline grammar, but no repository evidence shows the installed binary enforcing it. | A deliberately restrictive canary on hardware first, to prove the installed binary enforces a grammar at all, then a versioned protocol that pairs the grammar with the frozen action schema hash. | undetermined_pending_inspection | after QAIRT_EXPLICIT_SAMPLER_CONTROL |
| `DIAGNOSTIC_CANARY_SUBSET` | eval/run_secretary_eval.py resolves the split to whole dataset files. A subset is not expressible, and scripts/experiment_tracker.py only passes dev or all. | A separate diagnostic probe that loads development cases only, writes a clearly labelled DIAGNOSTIC_CANARY report, and is structurally incapable of producing a qualified archive. The frozen runner is not touched. The subset must be fixed before a session starts and never adapted to favour a candidate. | exposes_an_inference_control_only | optional for V1; startup probes plus dev35 are sufficient to launch |
| `FROZEN_PROMPT_AND_SCHEMA` | The system prompt and action schema are covered by frozen hashes (system_prompt_sha256, action_schema_sha256) that the tracker revalidates on every run. Changing either invalidates provenance identity with every historical result. | A new benchmark version with its own hashes, run as a separate protocol, never presented as identical to v2. | changes_benchmark_semantics | owner decision 6 governs this entirely |
| `DETERMINISTIC_ACTION_ROUTER` | turbo/secretary_proposal.py exists but is disabled by default, executes nothing and has only synthetic test evidence. | Keep it disabled inside the evaluation path. Any router that answers a benchmark case changes what the benchmark measures, so it needs its own protocol and its own reporting, not a config flag. | changes_benchmark_semantics | not before TurboLab V2 |
| `CODE_PATCH_CANDIDATES` | No sandbox, no restricted file set, no patch archive and no rollback path exists in this repository today. | A dedicated git worktree, an explicit allowlist of editable files, unit tests as a gate, a static diff review, the exact patch archived with the experiment, and an unconditional rollback afterwards. | undetermined_pending_inspection | explicitly V2; out of scope for the first implementation |

Full detail, including the files each item would touch and how it must be validated,
lives in `turbo/optimizer/lane_c.py`.
